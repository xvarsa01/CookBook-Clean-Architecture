<?xml version="1.0" encoding="utf-8"?>

<views:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:views="using:CookBook.CleanArch.Presentation.MauiApplication.Views"
                       xmlns:controls="using:CookBook.CleanArch.Presentation.MauiApplication.Controls"
                       xmlns:viewModels="using:CookBook.CleanArch.Presentation.MauiApplication.ViewModels"
                       xmlns:t="using:CookBook.CleanArch.Presentation.MauiApplication.Resources.Texts"
                       x:Class="CookBook.CleanArch.Presentation.MauiApplication.Views.Recipe.RecipeCreateView"
                       Title="{x:Static t:RecipeCreateViewTexts.Page_Title}"
                       Style="{StaticResource ContentPageStyle}"
                       x:DataType="viewModels:RecipeCreateViewModel">
    
    <controls:RecipeCreateEditBaseView
        Recipe="{Binding Recipe}"
        SaveCommand="{Binding SaveRecipeCommand}"
        ValidatePropertyCommand="{Binding ValidatePropertyCommand}"
        ToggleEditingCommand="{Binding ToggleEditingOfRecipeIngredientCommand}"
        Ingredients="{Binding Ingredients}"
        Units="{Binding Units}"
        FoodTypes="{Binding FoodTypes}"
        IngredientAmountNew="{Binding IngredientAmountNew}"
        SelectedNewIngredient="{Binding SelectedNewIngredient}"
        ValidateIngredientCommand="{Binding ValidateIngredientAmountNewPropertyCommand}"
        IngredientsEditingIsActive="{Binding IngredientsEditingIsActive}"
        IsExistingRecipe="{Binding IsExistingRecipe}"
        ToggleEditIngredientButtonText="{Binding ToggleEditIngredientButtonText}" />
    
</views:ContentPageBase>
